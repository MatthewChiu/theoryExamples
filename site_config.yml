# Site-wide settings.
#
# base_url: if you're publishing to https://<user>.github.io/<repo>/,
# set this to "/<repo>/" (with slashes on both ends). If you're using a
# custom domain or publishing to https://<user>.github.io/ directly,
# leave it as "/".
base_url: "/"
site_title: "The Analysis Index"
site_description: "A personal, growing catalog of music theory examples — sorted by composer, by piece, and by the harmonic and formal devices at work in them."
